/** Einheitliche Seiten-Abstände (Listen + Split-Layouts). */

/** Scroll-Seiten (PageContainer ohne fill) */
export const pagePadX = 'px-4 md:px-8';
export const pagePadBottom = 'pb-10';

/** Header unter Navbar — Titelzeile */
export const pageHeaderY = 'pt-6 pb-4';

/**
 * Split-Layouts (Kampagnen, Inbox, …) — PageContainer fill ist randlos.
 * Links und rechts in jeder Spalte gleich (außen = pagePadX).
 */
export const splitLeftPad = 'pl-4 md:pl-8';
export const splitLeftPadEnd = 'pr-4 md:pr-8';
export const splitMidPad = 'px-4 md:px-8';
export const splitRightPad = 'pl-4 md:pl-8 pr-4 md:pr-8';
export const splitHeaderY = pageHeaderY;
